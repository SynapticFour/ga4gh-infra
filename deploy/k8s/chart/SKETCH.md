This Helm chart is a **broker sketch**, not a supported production deploy.

Supported path: Docker Compose (`make up` / `docker/docker-compose.yml`).

Do not treat `helm upgrade --install` as GA4GH Infra on Kubernetes.

# Example configuration for the GA4GH DUO service.
# Copy to config/duo-service.toml and adjust for your deployment.

[server]
host = "0.0.0.0"
port = 8082
external_url = "https://duo.example.org"
environment = "dev"

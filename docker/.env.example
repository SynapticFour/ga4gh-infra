# Docker image version pins for docker/docker-compose.yml and docker-compose.sqlite.yml.
# Copy to docker/.env and adjust when mixing component versions.
# Image tags match crate versions (see docs/versioning.md).

GA4GH_IMAGE_PREFIX=ghcr.io/synapticfour

AAI_BROKER_VERSION=0.1.0
VISA_REGISTRY_VERSION=0.1.0
DUO_SERVICE_VERSION=0.1.0
SERVICE_REGISTRY_VERSION=0.1.0
SAMPLE_RESOURCE_VERSION=0.1.0
MOCK_IDP_VERSION=0.1.0
GA4GH_INFRA_VERSION=0.1.0
